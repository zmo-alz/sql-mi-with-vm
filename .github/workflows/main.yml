name: Main GH Action Template
on:
  pull_request:
    branches:
    - main
  push:
    branches:
    - main
  workflow_dispatch: {}
jobs:
  PSRule:
    name: PS Rule ARM Template Checker
    id: custom-action
    uses: zmo-alz/sql-mi-with-vm/.github/workflows/PSRule.yml
  ARMdeployment:
    name: ARM Template Deploy
    id: custom-action2
    uses: zmo-alz/sql-mi-with-vm/.github/workflows/ARMdeployment.yml
